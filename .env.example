# Database (Neon or Supabase Postgres connection string)
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"

# Auth (we will configure Better Auth or simple sessions)
# BETTER_AUTH_SECRET="your-long-random-secret-here"
# BETTER_AUTH_URL="http://localhost:3000"

# Public site URL (update when going live on testingzone.live)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Optional for image uploads later
# BLOB_READ_WRITE_TOKEN=
